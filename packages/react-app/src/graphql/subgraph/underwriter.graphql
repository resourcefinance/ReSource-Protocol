query getTotalCollateral($id: ID!) {
  underwriter(id: $id) {
    totalCollateral
  }
}

query getUnderwriter($id: ID!) {
  underwriter(id: $id) {
    totalRewards
    creditLines {
      ...CreditLineFields
      underwriter {
        id
      }
    }
  }
}
