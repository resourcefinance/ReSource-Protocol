query getTotalCollateral($id: ID!) {
  underwriter(id: $id) {
    totalCollateral
  }
}

query getPortfolio($id: ID!) {
  underwriter(id: $id) {
    totalRewards
    creditLines {
      collateral
      totalReward
      outstandingReward
      networkToken
      underwriter {
        id
      }
    }
  }
}



