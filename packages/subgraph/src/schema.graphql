type CreditLine @entity {
  id: ID!
  underwritee: Bytes!
  underwriter: Bytes!
  collateral: BigInt!
  networkToken: Bytes!
  issueDate: BigInt!
  outstandingReward: BigInt!
  totalReward: BigInt!
}

type Underwriter @entity {
  id: ID!
  creditLines: CreditLine[]!
  totalRewards: BigInt!
}