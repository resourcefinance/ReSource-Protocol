type CreditLine @entity {
  id: ID!
  underwritee: Bytes!
  underwriter: Bytes!
  collateral: BigInt!
  networkToken: Bytes!
  issueDate: BigInt!
  outstandingReward: BigInt!
  totalReward: BigInt!
}
